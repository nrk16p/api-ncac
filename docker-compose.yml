# รัน api-ncac (มี LibreOffice ในตัว) — แปลง docx→PDF ได้เองในตัว service เดียว
#   docker compose up -d   →   http://localhost:8000
services:
  api:
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
